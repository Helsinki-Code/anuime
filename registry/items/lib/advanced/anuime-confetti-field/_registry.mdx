---
name: anuime-confetti-field
type: registry:ui
title: "AnUIme Confetti Field"
description: "A one-shot success field that rejects generic party-confetti styling."
localRegistryDependencies:
  - anuime-recipe
files:
  - path: anuime-confetti-field.tsx
    type: registry:ui
---

Expressive success tier. Kira uses sparse `kira.shard` fragments on `kira.fringe`; Mochi uses fine `mochi.ribbon` rules with at most one `mochi.star`; Atlas uses measured `atlas.strap` and `atlas.grid` fragments.

```tsx
import { AnuimeConfettiField } from "@/components/ui/anuime-confetti-field";
```

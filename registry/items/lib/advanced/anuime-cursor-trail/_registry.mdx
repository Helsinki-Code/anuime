---
name: anuime-cursor-trail
type: registry:ui
title: "AnUIme Cursor Trail"
description: "A finite, optional cursor trace constrained by character law."
localRegistryDependencies:
  - anuime-recipe
files:
  - path: anuime-cursor-trail.tsx
    type: registry:ui
---

Expressive ambient tier. Kira traces `kira.circuit`/`kira.fringe`, Mochi permits at most three `mochi.pearl` points on `mochi.drape`, and Atlas follows `atlas.grid`/`atlas.beam`. The native cursor remains intact.

```tsx
import { AnuimeCursorTrail } from "@/components/ui/anuime-cursor-trail";
```

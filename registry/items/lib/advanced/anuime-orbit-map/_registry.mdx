---
name: anuime-orbit-map
type: registry:ui
title: "AnUIme Orbit Map"
description: "A restrained character-aware orbit for waiting and ambient system states."
localRegistryDependencies:
  - anuime-recipe
files:
  - path: anuime-orbit-map.tsx
    type: registry:ui
---

Expressive waiting/ambient tier. Its carriers are `kira.collar` + `kira.circuit`, `mochi.crescent` + `mochi.pearl`, and `atlas.core` + `atlas.grid`.

```tsx
import { AnuimeOrbitMap } from "@/components/ui/anuime-orbit-map";
```

---
name: anuime-scramble-text
type: registry:ui
title: "AnUIme Scramble Text"
description: "A deterministic transition text effect with character-authored resolution behavior."
localRegistryDependencies:
  - anuime-recipe
files:
  - path: anuime-scramble-text.tsx
    type: registry:ui
---

Kira resolves through `kira.shard` and `kira.circuit`; Mochi uses the explicit geometry-only restraint from `mochi.gracious-never-sweet`; Atlas resolves on `atlas.grid`.

```tsx
import { AnuimeScrambleText } from "@/components/ui/anuime-scramble-text";
```

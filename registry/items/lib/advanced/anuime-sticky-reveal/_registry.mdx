---
name: anuime-sticky-reveal
type: registry:ui
title: "AnUIme Sticky Reveal"
description: "A one-shot character-aware reveal that settles into stable reading order."
localRegistryDependencies:
  - anuime-recipe
files:
  - path: anuime-sticky-reveal.tsx
    type: registry:ui
---

Expressive transition tier. The reveal maps to `kira.fringe`/`kira.collar`, `mochi.drape`/`mochi.veil`, or `atlas.beam`/`atlas.panel`, using the approved 1700 ms text cadence.

```tsx
import { AnuimeStickyReveal } from "@/components/ui/anuime-sticky-reveal";
```

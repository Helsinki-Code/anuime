---
name: anuime-parallax-layer
type: registry:ui
title: "AnUIme Parallax Layer"
description: "A localized pointer-linked depth field constrained to each character's authored direction."
localRegistryDependencies:
  - anuime-recipe
files:
  - path: anuime-parallax-layer.tsx
    type: registry:ui
---

Expressive ambient tier. Direction and carriers trace to `kira.fringe` at 114°, `mochi.veil`/`mochi.drape`, and `atlas.beam` at 76°. Reduced motion freezes every layer.

```tsx
import { AnuimeParallaxLayer } from "@/components/ui/anuime-parallax-layer";
```
